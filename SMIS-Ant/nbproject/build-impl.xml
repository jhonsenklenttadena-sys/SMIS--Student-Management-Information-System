<?xml version="1.0" encoding="UTF-8"?>
<!--
*** GENERATED FILE — DO NOT EDIT DIRECTLY ***
NetBeans will regenerate this file if needed.
-->
<project xmlns:j2seproject1="http://www.netbeans.org/ns/j2se-project/1"
         xmlns:j2seproject3="http://www.netbeans.org/ns/j2se-project/3"
         xmlns:jaxrpc="http://www.netbeans.org/ns/jax-rpc"
         basedir=".."
         name="SMIS-impl">

    <property file="nbproject/private/config.properties"/>
    <property file="nbproject/private/configs/${config}.properties"/>
    <property file="nbproject/private/private.properties"/>
    <property file="nbproject/project.properties"/>

    <!-- ========== INIT ========== -->
    <target name="-pre-init"/>
    <target name="-do-init" depends="-pre-init">
        <property name="platform.home" value="${java.home}"/>
        <condition property="have.tests">
            <or>
                <available file="${test.src.dir}"/>
            </or>
        </condition>
        <condition property="no.javadoc.preview">
            <isfalse value="${javadoc.preview}"/>
        </condition>
        <property name="run.jvmargs" value=""/>
        <property name="javac.compilerargs" value=""/>
        <property name="work.dir" value="${basedir}"/>
        <condition property="no.deps">
            <and>
                <istrue value="${no.dependencies}"/>
            </and>
        </condition>
    </target>
    <target name="-post-init"/>
    <target name="init" depends="-pre-init,-do-init,-post-init"/>

    <!-- ========== COMPILE ========== -->
    <target name="-pre-pre-compile"/>
    <target name="-pre-compile"/>

    <target name="-do-depend" if="do.depend">
        <depend srcdir="${src.dir}" destdir="${build.classes.dir}"
                cache="${build.dir}/depcache" includes="${includes}" excludes="${excludes}">
            <classpath>
                <path path="${javac.classpath}"/>
            </classpath>
        </depend>
    </target>

    <target name="-do-compile" depends="init,-pre-pre-compile,-pre-compile,-do-depend">
        <mkdir dir="${build.classes.dir}"/>
        <!-- Copy non-java files (resources like CSS) -->
        <copy todir="${build.classes.dir}">
            <fileset dir="${src.dir}" excludes="**/*.java"/>
        </copy>
        <javac srcdir="${src.dir}"
               destdir="${build.classes.dir}"
               classpath="${javac.classpath}"
               debug="true"
               deprecation="${javac.deprecation}"
               encoding="UTF-8"
               includeantruntime="false"
               source="${javac.source}"
               target="${javac.target}">
            <compilerarg line="${javac.compilerargs}"/>
        </javac>
    </target>
    <target name="-post-compile"/>
    <target name="compile" depends="-do-compile,-post-compile"
            description="Compile project sources."/>

    <!-- ========== JAR ========== -->
    <target name="-pre-pre-jar"/>
    <target name="-pre-jar"/>
    <target name="-do-jar-without-manifest" depends="compile" unless="manifest.available">
        <mkdir dir="${dist.dir}"/>
        <jar compress="${jar.compress}" destfile="${dist.jar}">
            <fileset dir="${build.classes.dir}"/>
            <manifest>
                <attribute name="Main-Class" value="${main.class}"/>
            </manifest>
        </jar>
    </target>
    <target name="-do-jar-with-manifest" depends="compile" if="manifest.available">
        <mkdir dir="${dist.dir}"/>
        <jar compress="${jar.compress}" destfile="${dist.jar}" manifest="${manifest.file}">
            <fileset dir="${build.classes.dir}"/>
        </jar>
    </target>
    <target name="-check-manifest">
        <available file="${manifest.file}" property="manifest.available"/>
    </target>
    <target name="-post-jar"/>
    <target name="jar" depends="-pre-pre-jar,-pre-jar,-check-manifest,-do-jar-with-manifest,-do-jar-without-manifest,-post-jar"
            description="Build JAR."/>

    <!-- ========== CLEAN ========== -->
    <target name="-pre-clean"/>
    <target name="-do-clean" depends="-pre-clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}" followsymlinks="false" includeemptydirs="true"/>
    </target>
    <target name="-post-clean"/>
    <target name="clean" depends="-do-clean,-post-clean"
            description="Clean build products."/>

    <!-- ========== RUN ========== -->
    <target name="-pre-run-single"/>
    <target name="-do-run" depends="compile">
        <java classname="${main.class}"
              fork="true"
              dir="${work.dir}">
            <classpath>
                <path path="${run.classpath}"/>
            </classpath>
            <jvmarg line="${run.jvmargs}"/>
        </java>
    </target>
    <target name="-post-run-single"/>
    <target name="run" depends="-do-run"
            description="Run project."/>

    <!-- ========== DEBUG ========== -->
    <target name="-debug-start-debuggee" depends="compile">
        <java classname="${main.class}"
              fork="true"
              dir="${work.dir}">
            <classpath>
                <path path="${debug.classpath}"/>
            </classpath>
            <jvmarg value="-Xdebug"/>
            <jvmarg value="-Xrunjdwp:transport=dt_socket,address=${jpda.address}"/>
            <jvmarg line="${run.jvmargs}"/>
        </java>
    </target>
    <target name="debug" depends="-debug-start-debuggee"
            description="Debug project in IDE."/>

    <!-- ========== JAVADOC ========== -->
    <target name="-pre-javadoc"/>
    <target name="-do-javadoc" depends="init">
        <mkdir dir="${dist.dir}/javadoc"/>
        <javadoc destdir="${dist.dir}/javadoc"
                 encoding="UTF-8"
                 source="${javac.source}">
            <classpath>
                <path path="${javac.classpath}"/>
            </classpath>
            <sourcepath>
                <pathelement path="${src.dir}"/>
            </sourcepath>
            <packageset dir="${src.dir}" includes="**"/>
        </javadoc>
    </target>
    <target name="-post-javadoc"/>
    <target name="javadoc" depends="-do-javadoc"
            description="Build Javadoc."/>

</project>
